name: Checks

on:
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: checks-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  bun-linux:
    name: Bun checks on Linux
    runs-on: ubuntu-24.04
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.4.0

      - name: Cache Bun install cache
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-1.4.0-${{ hashFiles('bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-1.4.0-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Typecheck workspace
        run: bun run typecheck

      - name: Test workspace
        run: bun run test

      - name: Build web app
        run: bun run web:build

      - name: Enforce deterministic M8 performance budget
        env:
          MINERADIO_PERF_SKIP_BUILD: "1"
          MINERADIO_PERF_SKIP_NATIVE: "1"
        run: bun run perf:budget

  rust-windows:
    name: Rust checks on Windows
    runs-on: windows-2022
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.4.0

      - name: Cache Bun install cache
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-1.4.0-${{ hashFiles('bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-1.4.0-

      - name: Install Rust 1.95.0
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"

          & rustup toolchain install 1.95.0 --profile minimal --component clippy --component rustfmt
          if ($LASTEXITCODE -ne 0) {
            throw "安装 Rust 1.95.0 失败"
          }

          & rustup default 1.95.0
          if ($LASTEXITCODE -ne 0) {
            throw "切换 Rust 1.95.0 失败"
          }

          $rustVersionOutput = @(& rustc --version)
          if ($LASTEXITCODE -ne 0) {
            throw "读取 Rust 版本失败"
          }

          $rustVersion = ($rustVersionOutput -join "").Trim()
          if ($rustVersion -notmatch '^rustc 1\.95\.0(?:\s|$)') {
            throw "Rust 版本不匹配: $rustVersion"
          }

      - name: Cache Cargo downloads
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: cargo-downloads-${{ runner.os }}-1.95.0-${{ hashFiles('apps/desktop/src-tauri/Cargo.lock') }}
          restore-keys: |
            cargo-downloads-${{ runner.os }}-1.95.0-

      - name: Install dependencies
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          & bun install --frozen-lockfile
          if ($LASTEXITCODE -ne 0) {
            throw "安装依赖失败"
          }

      - name: Build web app for Tauri checks
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          & bun run web:build
          if ($LASTEXITCODE -ne 0) {
            throw "构建 Web 应用失败"
          }

      - name: Validate Cargo metadata
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          & cargo metadata --manifest-path apps/desktop/src-tauri/Cargo.toml --locked --format-version 1 --no-deps
          if ($LASTEXITCODE -ne 0) {
            throw "Cargo metadata 校验失败"
          }

      - name: Check Rust formatting
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          & cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml --all -- --check
          if ($LASTEXITCODE -ne 0) {
            throw "Rust 格式校验失败"
          }

      - name: Lint Rust targets
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          & cargo clippy --manifest-path apps/desktop/src-tauri/Cargo.toml --all-targets --all-features --locked -- -D warnings
          if ($LASTEXITCODE -ne 0) {
            throw "Rust Clippy 校验失败"
          }

      - name: Test Rust workspace
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          & cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml --locked
          if ($LASTEXITCODE -ne 0) {
            throw "Rust 测试失败"
          }

      - name: Test updater signature verifier
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          & cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml --locked --example verify_updater_signature
          if ($LASTEXITCODE -ne 0) {
            throw "updater 签名 verifier 测试失败"
          }
